#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#include "brooks.h"

Filiais *ultimaFilial = NULL;


int main ()
{
    int id;
    for (;;)
    {
        printf("id nova filial ");
        scanf("%d", &id);
        if (id == -1) break;
        cadNovaFilial(id,'c','c');
    }
    impressaoFiliais( ultimaFilial);

    return 0;
}

